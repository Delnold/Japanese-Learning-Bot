const hiraganaPrac = 'ひらがな　Hiragana'
const katakanaPrac = 'カタカナ　Katakana'

const katahiraPracRegEx = new RegExp(katakanaPrac + "|" + hiraganaPrac)

export {hiraganaPrac, katakanaPrac, katahiraPracRegEx}